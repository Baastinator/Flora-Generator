import { NumToInchPipe } from './num-to-inch.pipe';

describe('NumToInchPipe', () => {
  it('create an instance', () => {
    const pipe = new NumToInchPipe();
    expect(pipe).toBeTruthy();
  });
});
